\fancyhead{}
\fancyfoot{}
\cfoot{\thepage}


\lhead{Método}

\chapter{Método}

Este capítulo tiene como propósito detallar el enfoque metodológico y técnico seguido para el desarrollo del "Sistema de Asistencia Basado en Reconocimiento Facial para la FPUNE". Se presenta la planificación general del proyecto, desde la definición de los requisitos iniciales hasta la validación y evaluación del sistema implementado en un entorno académico real.

Este capítulo constituye un componente fundamental para comprender la implementación técnica del proyecto y validar su efectividad en relación con los objetivos propuestos.

En primer lugar, se describe el enfoque metodológico adoptado, el cual combina técnicas cualitativas y cuantitativas bajo un esquema de investigación aplicada tecnológica, permitiendo así un análisis integral del problema y su solución. Posteriormente, se exponen las fases específicas del proceso de desarrollo, tales como la recolección y preprocesamiento de datos, la selección de algoritmos, el entrenamiento del modelo, y la integración del sistema en un entorno controlado.


\section{Enfoque}

El presente trabajo se caracteriza por ser una investigación aplicada de tipo tecnológico, orientada al desarrollo de un sistema automatizado de control de asistencia basado en reconocimiento facial.

El enfoque de análisis de datos es mixto, combinando métodos cuantitativos y cualitativos. El primero permite medir la precisión y el rendimiento del sistema, mientras que el segundo considera la percepción de los usuarios sobre su facilidad de uso y utilidad en el entorno académico.

El alcance de la investigación es de tipo descriptivo, ya que se analizan los componentes, el funcionamiento y los resultados obtenidos durante la implementación del sistema.

El diseño de la investigación es no experimental y longitudinal, dado que el sistema será implementado y evaluado a lo largo de un semestre académico, permitiendo observar su desempeño y evolución en diferentes etapas.


\section{Método utilizado para el desarrollo}

El desarrollo de este trabajo se basó en la metodología ágil \textbf{Scrum}, la cual se centra en la planificación iterativa y la entrega progresiva de resultados funcionales. Este enfoque permite organizar el proyecto en ciclos cortos de trabajo denominados \textit{sprints}, donde se desarrollan y mejoran las distintas funcionalidades del sistema.\cite{atlassianScrum2024}

Scrum se fundamenta en los principios de transparencia, inspección y adaptación, promoviendo la colaboración y la comunicación continua entre los miembros del equipo. En este proyecto, se realizaron reuniones semanales para planificar tareas, revisar avances y establecer los objetivos del siguiente ciclo.

% ... último párrafo de "Método utilizado para el desarrollo"

\begin{figure}[H]
  \centering
  \includegraphics[width=0.99\textwidth]{imagenes_doc/Sprints.jpg}
  \caption{Ciclo de desarrollo bajo la metodología ágil Scrum.}
  \label{fig:scrum}
\end{figure}




\section{Fases del Proceso Investigativo}

\begin{itemize}
  \item \textbf{Identificación requisitos del proyecto} \\
  Se identificaron las necesidades y objetivos del sistema, considerando el número de usuarios, el entorno de aplicación, la precisión requerida y las condiciones de iluminación propias de las aulas de la FPUNE. Esta etapa permitió definir los requerimientos funcionales y no funcionales, así como los recursos técnicos necesarios para el desarrollo.

   \item \textbf{Recolección y preparación de datos} \\
  Se llevó a cabo la recopilación de imágenes faciales de los usuarios en distintas condiciones de iluminación, expresiones y ángulos, con el fin de conformar una base de datos representativa. Las imágenes fueron preprocesadas mediante normalización, eliminación de ruido y ajustes de brillo y contraste.

  \newpage
  \item \textbf{Entrenamiento del modelo} \\
  Utilizando las imágenes preprocesadas, se entrenó el modelo para generar representaciones vectoriales (embeddings) únicas de cada rostro, lo que permite su posterior comparación y verificación. Esta fase fue esencial para optimizar la precisión del reconocimiento facial.

  \item \textbf{Diseño del sistema} \\
  Se definió la arquitectura modular del sistema, integrando los componentes de captura, procesamiento, reconocimiento facial y registro en la base de datos. Además, se diseñó la base de datos relacional para almacenar tanto los datos de los usuarios como los registros de asistencia.

 
  \item \textbf{Pruebas e implementación} \\
  Se realizaron pruebas funcionales en entornos controlados para evaluar el desempeño del sistema. Posteriormente, se implementó una prueba piloto en un aula de la FPUNE, ajustando parámetros de reconocimiento para mejorar la precisión y reducir los falsos positivos.
  
\end{itemize}


\section{Análisis y selección de herramientas}

Durante esta fase se realizó un análisis comparativo de diversas tecnologías y librerías aplicadas en el desarrollo de sistemas de reconocimiento facial. La selección de herramientas se basó en criterios como precisión, rendimiento, compatibilidad, facilidad de integración y disponibilidad de documentación. 

A continuación, se detallan las principales herramientas seleccionadas:

\begin{itemize}

\item \textbf{Modelos de detección y reconocimiento facial:} \\
Se analizaron alternativas basadas en aprendizaje profundo, priorizando aquellas que ofrecen un equilibrio entre precisión y velocidad de procesamiento. Se optó por el uso de \textbf{YOLO-Face} para la detección de rostros en tiempo real, y por la librería \textbf{DeepFace} con el modelo \textbf{ArcFace} para la verificación facial, debido a su alta exactitud y estabilidad ante variaciones de iluminación, ángulo y expresión.

\item \textbf{Framework de desarrollo backend:} \\
Para la construcción de la API y la gestión de la lógica del sistema se empleó \textbf{Flask}, un microframework de Python que permite desarrollar aplicaciones ligeras y escalables. Su estructura modular facilita la integración con librerías de visión por computadora y bases de datos.

\item \textbf{Framework de desarrollo frontend:} \\
La interfaz de usuario fue desarrollada en \textbf{React}, permitiendo una interacción dinámica y fluida con el sistema. Su arquitectura basada en componentes favorece la reutilización del código y la visualización en tiempo real de los registros de asistencia.

\item \textbf{Base de datos:} \\
Se utilizó \textbf{PostgreSQL} para el almacenamiento estructurado de la información, incluyendo los registros de asistencia y datos de usuarios. Su robustez y soporte para operaciones concurrentes la convierten en una opción óptima para sistemas de este tipo.

\item \textbf{Herramienta de pruebas de API:} \\
Durante el proceso de desarrollo e integración, se empleó \textbf{Postman} para realizar pruebas de comunicación entre el frontend y el backend. Esta herramienta permitió verificar el correcto funcionamiento de las rutas, peticiones y respuestas del sistema antes de su despliegue final.

\end{itemize}

La combinación de estas herramientas permitió un desarrollo eficiente, garantizando la integración adecuada entre los módulos de detección, reconocimiento y gestión de asistencia dentro del sistema.


\section{Recolección de datos}

Para el funcionamiento del sistema de reconocimiento facial, fue necesario disponer de un conjunto de imágenes faciales representativas de los usuarios. Durante el registro de cada nuevo usuario, el sistema captura un mínimo de cinco imágenes desde diferentes ángulos y expresiones, con el fin de mejorar la capacidad de reconocimiento en distintas condiciones de uso.

Las imágenes se obtienen con cámaras externas (teléfonos móviles, webcams o cámaras digitales) y posteriormente se cargan al sistema mediante el módulo de registro de nuevos estudiantes. Durante este proceso se verifica el formato y un tamaño mínimo recomendado para preservar los rasgos faciales. Cada archivo se organiza en una carpeta individual asociada al usuario correspondiente y se nombra de forma sistemática, facilitando su identificación y el procesamiento posterior.

En la Figura~\ref{fig:rostro_original} se muestra un ejemplo de una imagen facial capturada al natural durante el proceso de registro, mientras que la Figura~\ref{fig:rostro_normalizado} presenta el resultado tras aplicar el preprocesamiento correspondiente.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.99\textwidth]{imagenes_doc/sin_normalizar.png}
    \caption{Ejemplo de imagen facial capturada al natural.}
    \label{fig:rostro_original}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.99\textwidth]{imagenes_doc/dataset_rostros.jpg}
    \caption{Ejemplo de imagen facial luego del proceso de normalización.}
    \label{fig:rostro_normalizado}
\end{figure}

El proceso de preprocesamiento incluye la detección automática del rostro, recorte, alineación, y ajustes de brillo y contraste. Este procedimiento asegura que todas las imágenes presenten condiciones similares de entrada, mejorando la precisión del modelo en la identificación de los usuarios en tiempo real.


\section{Entrenamiento del Modelo}

El entrenamiento del modelo constituye una etapa esencial del sistema, ya que permite generar representaciones numéricas únicas (\textit{embeddings}) de cada rostro a partir de las imágenes registradas. En esta fase se empleó la librería \textbf{DeepFace} junto con el modelo \textbf{ArcFace}, reconocido por su alta precisión en tareas de verificación facial gracias a su capacidad para generar embeddings discriminativos y estables frente a variaciones de iluminación, expresión o ángulo de captura.

\subsection*{Configuración del entorno de entrenamiento}

\begin{itemize}
    \item \textbf{Lenguaje:} Python 3.12.11
    \item \textbf{Librerías principales:} DeepFace, OpenCV, NumPy, Ultralytics
    \item \textbf{Entorno:} Visual Studio Code (ejecución local con GPU o CPU)
    \item \textbf{Estructura del dataset:} Imágenes organizadas por usuario en carpetas dentro de \texttt{raw\_faces/} (imágenes originales) y \texttt{known\_faces/} (rostros recortados).
    \item \textbf{Formato de salida:} Archivos JSON con los embeddings y centroides por usuario.
\end{itemize}

\subsection*{Proceso de entrenamiento}

El proceso de entrenamiento fue automatizado mediante el script \texttt{train\_faces.py}, compuesto por dos fases principales: el preprocesamiento de imágenes y la generación de representaciones faciales.

\begin{enumerate}
    \item \textbf{Registro del usuario e ingreso de imágenes:} \\
    Desde la interfaz web, un usuario administrativo registra a un nuevo alumno y carga sus fotografías faciales. Cada conjunto de imágenes se guarda en una carpeta individual dentro del directorio \texttt{raw\_faces/}, identificada por el número de cédula del alumno (CI).

    \item \textbf{Detección y recorte facial:} \\
    Para garantizar que solo se utilicen regiones válidas del rostro, el sistema emplea el modelo \textbf{YOLO-Face}. Este detecta automáticamente el rostro en cada imagen, recorta la región correspondiente y la redimensiona a \textbf{160×160 píxeles}, almacenando el resultado en \texttt{known\_faces/}. Este paso estandariza las entradas, asegurando consistencia en las condiciones de entrenamiento.

    \item \textbf{Generación de embeddings con DeepFace:} \\
    Cada imagen procesada se analiza mediante \textbf{DeepFace} utilizando el modelo \textbf{ArcFace}, el cual genera un vector numérico de características que describe de forma única los rasgos faciales del individuo. \\
    Para cada alumno, el sistema genera un archivo JSON individual con su número de cédula como nombre (por ejemplo, \texttt{5144692.json}), que contiene todos los embeddings derivados de sus imágenes.

    \item \textbf{Cálculo y actualización de centroides:} \\
    Posteriormente, se calcula el \textbf{centroide} del alumno, representando el promedio robusto de todos sus embeddings. Este vector resume los rasgos característicos del rostro y facilita comparaciones futuras. \\
    A diferencia de un reentrenamiento completo, el sistema implementa una \textbf{actualización incremental}: cuando se agrega un nuevo alumno o se añaden más imágenes a un registro existente, el sistema no recalcula todos los centroides, sino que actualiza únicamente el correspondiente al usuario modificado. De este modo, se optimiza el rendimiento y se conserva el historial de entrenamiento.

    \item \textbf{Almacenamiento estructurado:} \\
    Los embeddings individuales se guardan en el directorio \texttt{embeddings/}, mientras que el archivo \texttt{centroids.json} mantiene un índice general con los centroides y estadísticas de todos los usuarios. Esta estructura facilita la expansión progresiva del sistema sin afectar los registros previos.
\end{enumerate}


\section{Diseño de la aplicación}
\subsection{Arquitectura del sistema}

El sistema fue diseñado bajo el enfoque cliente-servidor, utilizando una arquitectura basada en \textbf{REST API} para permitir la comunicación fluida entre el \textbf{Frontend (cliente)} y el \textbf{Backend (servidor)}.  
Esta arquitectura facilita la escalabilidad, el mantenimiento y la integración con futuras funcionalidades del sistema de reconocimiento facial.

El sistema se compone de tres módulos principales:

\begin{itemize}
    \item \textbf{Frontend:} desarrollado en \textbf{React.js}, se encarga de la interfaz gráfica y la interacción directa con el usuario. Permite registrar nuevos estudiantes, gestionar materias, asignarlas a los alumnos, capturar o subir fotografías faciales, consultar los registros de asistencia y cargar justificativos en caso de inasistencia. Su diseño adaptable y responsivo garantiza una experiencia de uso fluida tanto en equipos de escritorio como en dispositivos móviles.

    \item \textbf{Backend:} implementado en \textbf{Flask (Python)}, maneja la lógica del sistema, las rutas de la API y la comunicación con la base de datos. Integra los módulos de detección facial con \textbf{YOLO-Face} y el reconocimiento mediante \textbf{DeepFace (modelo ArcFace)}, además de gestionar el almacenamiento de embeddings, centroides y registros de asistencia.

    \item \textbf{Base de datos:} construida en \textbf{PostgreSQL}, almacena de forma estructurada la información de usuarios, cursos, asistencias y datos biométricos. Se eligió por su robustez, soporte de transacciones concurrentes y compatibilidad con entornos productivos en la nube.
\end{itemize}
\subsection{Integración de IA y Reconocimiento Facial}

Para la detección y reconocimiento facial, el sistema integra dos componentes principales de inteligencia artificial: \textbf{YOLO-Face}, una red neuronal convolucional especializada en la detección de rostros en tiempo real, y \textbf{DeepFace} con el modelo \textbf{ArcFace}, encargado de generar las representaciones numéricas (embeddings) y verificar la identidad de los usuarios.  

El modelo \textbf{YOLO-Face} identifica y localiza las caras presentes en cada imagen o cuadro de video. Una vez detectado el rostro, el sistema recorta automáticamente la zona correspondiente y la normaliza a un tamaño estándar de \textbf{160×160 píxeles}, garantizando uniformidad en las condiciones de entrada para la siguiente etapa del procesamiento.  

Posteriormente, el rostro recortado es analizado mediante \textbf{DeepFace}, que genera un vector de características (embedding) utilizando el modelo \textbf{ArcFace}. Estos vectores se almacenan y utilizan en fases posteriores del sistema para la identificación de los usuarios registrados.  

\textbf{El flujo general de procesamiento es el siguiente:}

\begin{enumerate}
    \item Captura o carga de imagen facial desde la interfaz.
    \item Detección automática de rostros mediante YOLO-Face.
    \item Recorte y normalización del rostro detectado.
    \item Generación de embeddings con DeepFace (modelo ArcFace).
    \item Registro automático de la asistencia según el resultado del proceso.
\end{enumerate}

El proceso completo se ejecuta desde el backend, implementado en \textbf{Flask (Python)}, que coordina la detección, el reconocimiento y el registro de resultados en la base de datos \textbf{PostgreSQL}.  
Esta integración permite un flujo automatizado, eficiente y en tiempo real, garantizando precisión en la identificación de los estudiantes durante la toma de asistencia.

\subsection{Relevamiento de requisitos}

El relevamiento de requisitos se llevó a cabo mediante la aplicación de encuestas dirigidas a profesores y estudiantes de la Facultad Politécnica de la Universidad Nacional del Este (FPUNE), con el objetivo de identificar las principales necesidades y expectativas respecto al control de asistencia en el entorno académico.  

Los resultados obtenidos permitieron comprender las mejoras asociadas a los métodos tradicionales de control de asistencia —tales como pérdida de tiempo, errores humanos y registros inconsistentes—, y sirvieron de base para definir los requisitos funcionales y no funcionales del sistema.

\subsubsection*{Requisitos funcionales (RF)}

Los requisitos funcionales representan las acciones que el sistema debe realizar para cumplir su propósito. A partir del análisis de las encuestas y la definición de objetivos, se identificaron los siguientes requisitos funcionales, resumidos en la Tabla~\ref{tab:rf_checkface}.

\begin{table}[H]
\centering
\caption{Requisitos funcionales del sistema CheckFace}
\label{tab:rf_checkface}
\begin{tabular}{|c|p{11cm}|}
\hline
\textbf{Código} & \textbf{Requisito funcional} \\ \hline
RF1 & El sistema debe permitir el registro de nuevos estudiantes y docentes mediante una interfaz web. \\ \hline
RF2 & El sistema debe permitir la carga de imágenes faciales de los estudiantes para el entrenamiento del modelo. \\ \hline
RF3 & El sistema debe detectar automáticamente los rostros presentes en video. \\ \hline
RF4 & El sistema debe generar embeddings faciales y almacenarlos junto con la información del usuario. \\ \hline
RF5 & El sistema debe registrar automáticamente la asistencia al reconocer un rostro en tiempo real. \\ \hline
RF6 & El sistema debe permitir consultar y justificar asistencias desde la interfaz web. \\ \hline
\end{tabular}
\end{table}

\subsubsection*{Requisitos no funcionales (RNF)}

Los requisitos no funcionales establecen las condiciones de calidad, rendimiento y usabilidad que el sistema debe cumplir para garantizar un funcionamiento eficiente y confiable. La Tabla~\ref{tab:rnf_checkface} presenta los principales requisitos no funcionales definidos.

\begin{table}[H]
\centering
\caption{Requisitos no funcionales del sistema CheckFace}
\label{tab:rnf_checkface}
\begin{tabular}{|c|p{11cm}|}
\hline
\textbf{Código} & \textbf{Requisito no funcional} \\ \hline
RNF1 & El sistema debe procesar la detección y reconocimiento facial en menos de 3 segundos por usuario. \\ \hline
RNF2 & El sistema debe mantener una precisión mínima del 80\% en la identificación de rostros. \\ \hline
RNF3 & La interfaz debe ser intuitiva, responsiva y accesible desde dispositivos móviles y computadoras. \\ \hline
RNF4 & La base de datos debe garantizar integridad, seguridad y respaldo de los registros. \\ \hline
\end{tabular}
\end{table}


\subsection{Caso de Uso Principal del Sistema}

En la Figura~\ref{fig:caso_uso_principal} se representa el caso de uso principal del sistema, en el cual interactúan dos actores principales: el \textbf{docentes} y el \textbf{alumnos}.  

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{capitulo_04/imagenes/5.png}
    \caption{Caso de uso principal del sistema CheckFace.}
    \label{fig:caso_uso_principal}
\end{figure}

\subsubsection{Descripción general del caso de uso}

El sistema distingue dos tipos de usuarios con diferentes niveles de acceso: el \textbf{Docente} y el \textbf{Estudiante}.

\textbf{Docente:} tiene acceso completo a todas las funcionalidades del sistema. Puede registrar estudiantes, cursos, horarios y roles; asignar materias; registrar asistencias mediante el reconocimiento facial; y acceder al panel principal (\textit{Dashboard}) con los reportes y estadísticas. Además, puede administrar los datos del sistema y supervisar su funcionamiento general, actuando como un administrador.

\textbf{Estudiante:} dispone de un acceso limitado al sistema, pudiendo ingresar únicamente al \textit{Dashboard} para visualizar sus registros de asistencia y verificar su historial personal. No posee permisos de edición ni administración de datos.

\subsection{Definición de la Base de Datos y Tablas}

Para la gestión eficiente de la información del sistema, se diseñó una base de datos relacional implementada en \textbf{PostgreSQL}, compuesta por varias tablas que permiten almacenar, relacionar y gestionar los datos de usuarios, participantes, cursos, horarios y asistencias.  
La estructura propuesta busca garantizar integridad referencial, seguridad y escalabilidad, facilitando futuras ampliaciones del sistema.

\subsubsection*{Modelo de entidad-relación}

La base de datos se compone de siete entidades principales: \textbf{roles}, \textbf{users}, \textbf{participants}, \textbf{courses}, \textbf{course\_schedules}, \textbf{participant\_courses} y \textbf{attendances}.  
Estas entidades reflejan la estructura funcional del sistema, permitiendo asociar estudiantes a cursos, registrar asistencias y controlar los horarios académicos.  
En la Figura~\ref{fig:der_checkface} se muestra el diagrama entidad-relación correspondiente.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{capitulo_04/imagenes/1.png}
    \caption{Diagrama de Entidad–Relación del sistema CheckFace.}
    \label{fig:der_checkface}
\end{figure}

\subsubsection*{Diccionario de datos}

Con el fin de documentar detalladamente la estructura de cada tabla, se elaboró un diccionario de datos que especifica los atributos, tipos de datos y relaciones existentes. A continuación, se describen las tablas principales que conforman la base de datos.

\begin{table}[H]
\centering
\caption{Diccionario de datos de la tabla \textit{roles}}
\begin{tabular}{|l|l|p{8cm}|}
\hline
\textbf{Atributo} & \textbf{Tipo de Dato} & \textbf{Descripción} \\ \hline
id & SERIAL (PK) & Identificador único del rol. \\ \hline
description & VARCHAR(120) & Nombre o descripción del rol asignado al usuario. \\ \hline
\end{tabular}
\end{table}

\begin{table}[H]
\centering
\caption{Diccionario de datos de la tabla \textit{users}}
\begin{tabular}{|l|l|p{8cm}|}
\hline
\textbf{Atributo} & \textbf{Tipo de Dato} & \textbf{Descripción} \\ \hline
id & SERIAL (PK) & Identificador único del usuario. \\ \hline
username & VARCHAR(120) & Nombre de usuario utilizado para iniciar sesión. \\ \hline
password & TEXT & Contraseña encriptada del usuario. \\ \hline
role\_id & INT (FK) & Referencia al rol del usuario en la tabla \textit{roles}. \\ \hline
\end{tabular}
\end{table}

\begin{table}[H]
\centering
\caption{Diccionario de datos de la tabla \textit{participants}}
\begin{tabular}{|l|l|p{8cm}|}
\hline
\textbf{Atributo} & \textbf{Tipo de Dato} & \textbf{Descripción} \\ \hline
id & SERIAL (PK) & Identificador único del participante. \\ \hline
full\_name & VARCHAR(120) & Nombre completo del estudiante o participante. \\ \hline
ci & INT & Número de cédula de identidad del participante. \\ \hline
occupation & VARCHAR(50) & Rol académico del participante (por ejemplo, alumno). \\ \hline
user\_id & INT (FK) & Relación con el usuario correspondiente en la tabla \textit{users}. \\ \hline
\end{tabular}
\end{table}

\begin{table}[H]
\centering
\caption{Diccionario de datos de la tabla \textit{courses}}
\begin{tabular}{|l|l|p{8cm}|}
\hline
\textbf{Atributo} & \textbf{Tipo de Dato} & \textbf{Descripción} \\ \hline
id & SERIAL (PK) & Identificador único del curso. \\ \hline
name & VARCHAR(120) & Nombre de la materia o curso. \\ \hline
career & VARCHAR(120) & Carrera o programa académico al que pertenece el curso. \\ \hline
semester & INT & Semestre académico correspondiente. \\ \hline
\end{tabular}
\end{table}

\begin{table}[H]
\centering
\caption{Diccionario de datos de la tabla \textit{course\_schedules}}
\begin{tabular}{|l|l|p{8cm}|}
\hline
\textbf{Atributo} & \textbf{Tipo de Dato} & \textbf{Descripción} \\ \hline
id & SERIAL (PK) & Identificador único del horario. \\ \hline
course\_id & INT (FK) & Referencia al curso correspondiente. \\ \hline
day & VARCHAR(20) & Día de la semana en que se dicta el curso. \\ \hline
schedule & VARCHAR(50) & Horario asignado al curso. \\ \hline
classroom & VARCHAR(50) & Aula o sala asignada. \\ \hline
\end{tabular}
\end{table}

\begin{table}[H]
\centering
\caption{Diccionario de datos de la tabla \textit{participant\_courses}}
\begin{tabular}{|l|l|p{8cm}|}
\hline
\textbf{Atributo} & \textbf{Tipo de Dato} & \textbf{Descripción} \\ \hline
id & SERIAL (PK) & Identificador único de la relación. \\ \hline
participant\_id & INT (FK) & Referencia al participante matriculado. \\ \hline
course\_id & INT (FK) & Referencia al curso correspondiente. \\ \hline
\end{tabular}
\end{table}

\begin{table}[H]
\centering
\caption{Diccionario de datos de la tabla \textit{attendances}}
\begin{tabular}{|l|l|p{8cm}|}
\hline
\textbf{Atributo} & \textbf{Tipo de Dato} & \textbf{Descripción} \\ \hline
id & SERIAL (PK) & Identificador único del registro de asistencia. \\ \hline
participant\_id & INT (FK) & Referencia al participante que asiste al curso. \\ \hline
course\_id & INT (FK) & Referencia al curso al que pertenece la asistencia. \\ \hline
date & DATE & Fecha en la que se registra la asistencia. \\ \hline
observations & TEXT & Observaciones adicionales o justificación de inasistencia. \\ \hline
\end{tabular}
\end{table}


\subsection{Diagrama de flujo del funcionamiento del sistema}

A continuación, en la Figura~\ref{fig:flujo_checkface}, se presenta el diagrama de flujo que describe el funcionamiento general del sistema de asistencia basado en reconocimiento facial.  
Este diagrama representa las etapas principales del proceso automatizado, desde la carga de imágenes del alumno hasta el registro final de la asistencia en la base de datos.

En la primera etapa, el usuario (docente) carga las imágenes faciales de los estudiantes a través de la interfaz del sistema. Estas imágenes son procesadas por el script \texttt{train\_faces.py}, el cual realiza el entrenamiento inicial mediante la generación de embeddings faciales con el modelo \textbf{DeepFace (ArcFace)}.  
Una vez completado el entrenamiento, el sistema activa la cámara y da inicio al proceso de detección y reconocimiento en tiempo real utilizando el modelo \textbf{YOLO-Face}.

Durante la ejecución, si el rostro detectado coincide con un registro existente en la base de datos, el sistema marca automáticamente al estudiante como \textbf{presente}. En caso contrario, el rostro es clasificado como \textbf{desconocido}, permitiendo un control más preciso sobre la identificación facial.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{capitulo_04/imagenes/2.png}
    \caption{Diagrama de flujo del funcionamiento del sistema CheckFace.}
    \label{fig:flujo_checkface}
\end{figure}



\subsection{Desarrollo del frontend}

El frontend del sistema \textit{CheckFace} fue desarrollado utilizando \textbf{React} como framework principal y \textbf{Bootstrap} como librería de estilos. La elección de React permitió construir una interfaz modular basada en componentes reutilizables, mientras que Bootstrap facilitó la creación de un diseño responsivo, visualmente coherente y adaptable a diferentes tamaños de pantalla sin necesidad de definir hojas de estilo complejas desde cero. 

Esta combinación de tecnologías permitió desarrollar una interfaz clara, accesible y fácil de usar para todos los usuarios del sistema, garantizando una navegación intuitiva entre las distintas secciones y funcionalidades.


El desarrollo de la interfaz se organizó en módulos funcionales, cada uno representando una parte específica del sistema. La estructura general se compone de:
\begin{itemize}
  \item \textbf{Componentes de presentación:} responsables de mostrar la información y recibir datos de entrada mediante formularios, tablas y botones.
  \item \textbf{Ruteo de navegación:} implementado con \texttt{react-router-dom}, para permitir la transición fluida entre las distintas vistas del sistema.
  \item \textbf{Servicios:} encargados de establecer la comunicación con la API REST del backend, gestionando peticiones HTTP de forma centralizada.
  \item \textbf{Gestión de estado:} realizada mediante los hooks de React (\texttt{useState}, \texttt{useEffect}), asegurando la actualización dinámica de la información visualizada por el usuario.
\end{itemize}

\paragraph{Criterios de diseño de la interfaz.}
Durante el desarrollo del frontend se aplicaron los siguientes principios:
\begin{itemize}
  \item Consistencia visual en todos los módulos mediante el uso de componentes y clases de Bootstrap.
  \item Diseño \textit{responsive}, garantizando un correcto funcionamiento en diferentes resoluciones y dispositivos.
  \item Retroalimentación visual inmediata ante las acciones del usuario (por ejemplo, mediante alertas o indicadores de carga).
  \item Accesibilidad básica mediante etiquetas y descripciones que facilitan la interacción.
\end{itemize}

\paragraph{Flujo de uso.}
El flujo de interacción con el sistema contempla las siguientes acciones principales:
\begin{itemize}
  \item \textbf{Inicio de sesión:} los usuarios acceden al sistema ingresando sus credenciales personales a través de una interfaz simple y clara, garantizando que únicamente el personal autorizado pueda utilizar las funcionalidades.
  \item \textbf{Registro de nuevos usuarios:} se dispone de un formulario que permite crear nuevas cuentas, asignar roles y definir permisos.
  \item \textbf{Uso de los módulos principales:} dentro del panel lateral de navegación se encuentran las opciones \textit{Dashboard}, \textit{Reconocimiento Facial}, \textit{Registrar Estudiante}, \textit{Registrar Rol}, \textit{Registrar Curso}, \textit{Registrar Horario}, \textit{Registrar Asistencia} y \textit{Asignar Curso}.
\end{itemize}


- \textbf{Interfaz de inicio de sesión:} 
  Es el primer punto de acceso al sistema \textit{CheckFace}. 
  Su objetivo es permitir que los usuarios ingresen de forma segura mediante una pantalla sencilla y clara, desarrollada con \textbf{React} y estilizada con \textbf{Bootstrap}. 
  El formulario cuenta con dos campos principales (\texttt{Username} y \texttt{Password}), un botón de acceso y un enlace que dirige al formulario de registro en caso de no poseer una cuenta. 
  La autenticación se gestiona mediante una solicitud \texttt{POST} al backend implementado con \textbf{Flask}, el cual valida las credenciales antes de permitir el acceso.
  El diseño minimalista, con fondo claro y elementos centrados, ofrece una experiencia fluida tanto en computadoras como en dispositivos móviles.

  \begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{imagenes_doc/login.png}
    \caption{Interfaz de inicio de sesión del sistema CheckFace.}
    \label{fig:login}
  \end{figure}



- \textbf{Registro de usuario:}
  Permite crear una nueva cuenta mediante un formulario simple desarrollado con \textbf{React} y estilizado con \textbf{Bootstrap}. 
  Incluye los campos \texttt{Username} y \texttt{Password}, validaciones básicas (campos obligatorios y longitud mínima de la contraseña) y un botón de envío. 
  Al confirmar, se envía una solicitud \texttt{POST} al backend en \textbf{Flask} para almacenar de forma segura las credenciales. 
  La vista ofrece además un enlace para volver al inicio de sesión.

  \begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{imagenes_doc/new_user.png}
    \caption{Formulario de registro de usuario del sistema CheckFace.}
    \label{fig:new_user}
  \end{figure}



- \textbf{Dashboard:}
  Es la primera vista que se muestra tras el inicio de sesión en el sistema \textit{CheckFace}. 
  Su propósito es ofrecer una visión general del registro de asistencias de los estudiantes a través de gráficos y listados interactivos. 
  Esta sección centraliza la información y permite analizar la asistencia diaria aplicando distintos filtros según el curso y el rango de fechas seleccionado.

  En la Figura~\ref{fig:dashboard1} se observa un gráfico de barras que representa las \textit{asistencias por día}. 
  Los filtros ubicados en la parte superior permiten delimitar el período de consulta y visualizar únicamente los registros correspondientes a cada grupo. 
  Los datos presentados son de carácter demostrativo, utilizados exclusivamente para ilustrar el funcionamiento de la interfaz.

  \begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{imagenes_doc/dashboard_1.png}
    \caption{Gráfico de asistencias por día en el módulo Dashboard.}
    \label{fig:dashboard1}
  \end{figure}

  En la Figura~\ref{fig:dashboard2} se muestra la lista de estudiantes con sus asistencias registradas, curso correspondiente y porcentaje de asistencia. 
  La tabla incluye opciones para exportar los datos en distintos formatos (\texttt{CSV}, \texttt{XLSX} y \texttt{PDF}), lo que facilita la generación de reportes y su posterior análisis. 
  Los datos visualizados son únicamente de prueba y no representan información real.

  \begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{imagenes_doc/dashboard_2.png}
    \caption{Listado de estudiantes y asistencias registradas en el módulo Dashboard.}
    \label{fig:dashboard2}
  \end{figure}


  - \textbf{Reconocimiento facial:}
  este módulo permite realizar el proceso automático de detección y reconocimiento de rostros a través de la cámara del dispositivo. 
  Al iniciar el reconocimiento, el sistema analiza la imagen capturada en tiempo real, identifica el rostro y compara sus características con los registros existentes en la base de datos. 
  Si la persona es reconocida correctamente, el sistema muestra una confirmación visual y registra la asistencia correspondiente al curso asignado para ese día. 
  En caso contrario, se notifica al usuario que el rostro no fue reconocido.

  Esta funcionalidad permite automatizar el registro de asistencias, evitando el ingreso manual de datos y reduciendo el margen de error. 
  El flujo completo se ejecuta de forma local y en tiempo real, ofreciendo una experiencia fluida y precisa.

  \begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{imagenes_doc/reconocimiento_modulo.png}
    \caption{Módulo de reconocimiento facial automático del sistema CheckFace.}
    \label{fig:reconocimiento}
  \end{figure}


- \textbf{Registrar rol:}
  Permite crear nuevos roles del sistema y gestionar los existentes. 
  Incluye un formulario para ingresar la descripción del rol y una tabla con acciones de \textit{Editar} y \textit{Eliminar} sobre cada registro. 
  Ofrece validaciones básicas y confirma las operaciones realizadas para mantener la integridad de los datos.

  \begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{imagenes_doc/roles.png}
    \caption{Gestión de roles: registro, edición y eliminación.}
    \label{fig:roles}
  \end{figure}



- \textbf{Registrar curso:}
  Permite crear nuevos cursos indicando \textit{Nombre del curso}, \textit{Carrera} y \textit{Semestre}. 
  Presenta además un listado de cursos registrados con acciones de \textit{Editar} y \textit{Eliminar}, lo que facilita la administración de la oferta académica.

  \begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{imagenes_doc/new_curso.png}
    \caption{Registro y gestión de cursos.}
    \label{fig:new_curso}
  \end{figure}


  - \textbf{Registrar horario:}
  Permite asignar horarios a los cursos definidos en el sistema. 
  El formulario incluye los campos \textit{Curso}, \textit{Día}, \textit{Hora de inicio}, \textit{Hora de fin} y \textit{Aula}, garantizando la correcta organización de las clases. 
  En la parte inferior se muestra una tabla con los horarios registrados, que puede filtrarse por curso, día o aula, además de ofrecer opciones para \textit{Editar} o \textit{Eliminar} registros.

  \begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{imagenes_doc/horario_curso.png}
    \caption{Registro y gestión de horarios por curso.}
    \label{fig:horario_curso}
  \end{figure}


- \textbf{Registrar asistencia:}
  Permite ingresar asistencias de forma manual en casos excepcionales, como cuando un estudiante no fue reconocido por el sistema o necesita justificar una llegada tardía. 
  El formulario incluye los campos \textit{Estudiante}, \textit{Curso}, \textit{Fecha} y una sección opcional de \textit{Observación} para registrar detalles adicionales. 
  Este módulo garantiza que los registros de asistencia sean completos y precisos, incluso ante situaciones imprevistas.

  \begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{imagenes_doc/registrar_asistencia.png}
    \caption{Módulo de registro manual de asistencias.}
    \label{fig:registrar_asistencia}
  \end{figure}


- \textbf{Asignar curso:}
  Permite vincular a los participantes con los cursos disponibles en el sistema. 
  El formulario presenta los campos \textit{Participante} y \textit{Curso}, que se seleccionan de listas desplegables para asegurar la coherencia de los datos. 
  Esta funcionalidad facilita la organización académica al definir de manera precisa qué estudiantes pertenecen a cada curso.

  \begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{imagenes_doc/asignar_.png}
    \caption{Asignación de cursos a participantes.}
    \label{fig:asignar}
  \end{figure}



\subsection{Desarrollo del backend:}

El backend del sistema \textit{CheckFace} fue desarrollado con el framework \textbf{Flask} en \textbf{Python}, con el objetivo de ofrecer una capa de servicios ligera y modular que gestione la comunicación entre la interfaz web, la base de datos y los procesos de reconocimiento facial en tiempo real. 

La estructura del backend está organizada en carpetas según su función: \textit{routes} contiene los endpoints RESTful que conectan con el frontend; \textit{detection} realiza la detección facial mediante el modelo \textbf{YOLO-Face}; \textit{recognition} ejecuta el reconocimiento de rostros a partir de comparaciones vectoriales; y \textit{database} gestiona la conexión y operaciones con \textbf{PostgreSQL}. 
El archivo \texttt{api.py} centraliza la configuración del servidor y las rutas, mientras que los scripts \texttt{train\_faces.py} permiten entrenar y actualizar los registros faciales del sistema.

Las principales funcionalidades implementadas en esta capa incluyen:
\begin{itemize}
    \item \textbf{Exposición de endpoints RESTful:} Se definieron rutas HTTP para el manejo de usuarios, roles, cursos, horarios, asistencias y reconocimiento facial, enviando y recibiendo datos en formato JSON.
    \item \textbf{Procesamiento y reconocimiento facial:} Las imágenes capturadas desde el frontend son procesadas en el backend, donde se realiza la detección del rostro y su comparación con los vectores previamente registrados para registrar la asistencia correspondiente.
    \item \textbf{Gestión de usuarios y autenticación:} Se implementaron rutas dedicadas para el registro e inicio de sesión de usuarios, controlando el acceso a las distintas funciones del sistema.
    \item \textbf{Interacción con la base de datos:} Toda la información sobre usuarios, estudiantes, cursos, horarios y asistencias se gestiona en la base de datos \textbf{PostgreSQL}, mediante operaciones CRUD estructuradas.
\end{itemize}

Durante el desarrollo y las pruebas de los endpoints se utilizó la herramienta \textbf{Postman}, que permitió verificar la correcta comunicación entre el frontend y el backend, asegurando que las peticiones y respuestas cumplieran con el formato y comportamiento esperados.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.6\textwidth]{imagenes_doc/backend_estructura.png}
  \caption{Estructura general del proyecto CheckFace en Visual Studio Code.}
  \label{fig:backend_estructura}
\end{figure}

\subsubsection*{Líneas de código}

En el \textbf{ANEXO B} se presentan los fragmentos de código más relevantes del \textit{Sistema de Asistencia Basado en Reconocimiento Facial para la Facultad Politécnica de la Universidad Nacional del Este}, acompañados de una breve descripción que explica su funcionamiento dentro del proceso de reconocimiento facial. 

Los fragmentos seleccionados corresponden a los módulos principales del sistema: \texttt{train\_faces.py} y \texttt{recognizer.py}. 
El primero se encarga del entrenamiento de los rostros registrados, generando los vectores de características que serán utilizados posteriormente para la identificación de los estudiantes. 
El segundo contiene la lógica del reconocimiento facial en tiempo real, comparando los vectores generados con los almacenados en la base de datos y registrando la asistencia en caso de una coincidencia exitosa.  

Cada fragmento se acompaña de su respectiva explicación para facilitar la comprensión del flujo de ejecución, desde la lectura de las imágenes hasta la detección, verificación e identificación de los rostros dentro del entorno del aula.


\subsection{Usabilidad}

Para evaluar la usabilidad y el nivel de satisfacción de los usuarios con el \textit{Sistema de Asistencia Basado en Reconocimiento Facial para la Facultad Politécnica de la Universidad Nacional del Este}, se establecieron variables operacionales basadas en los principios de Experiencia de Usuario (UX), considerando la facilidad de uso, eficiencia, confiabilidad y aceptación general del sistema.

Como instrumento de medición se aplicó el cuestionario \textbf{SUS (System Usability Scale)}, ampliamente utilizado para obtener un puntaje global de usabilidad. 
La evaluación se realizó con una muestra conformada por docentes de la institución, quienes realizaron tareas representativas como el registro de usuarios, la toma de asistencia mediante reconocimiento facial y la verificación de reportes, todo dentro del entorno web del sistema.

Los datos recolectados permitieron determinar el nivel de aceptación del sistema y detectar posibles oportunidades de mejora en la interfaz y el flujo de uso. 
Los resultados consolidados y las respuestas individuales se presentan en el \textbf{ANEXO A} correspondiente.
