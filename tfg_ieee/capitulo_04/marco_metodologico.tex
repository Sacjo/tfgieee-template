\fancyhead{}
\fancyfoot{}
\cfoot{\thepage}


\lhead{Método}

\chapter{Método}

Se presenta la planificación general del trabajo, desde la definición de los requisitos iniciales hasta la validación y evaluación del prototipo en un entorno académico real con un grupo de \textit{N}=13 estudiantes participantes.

En primer lugar, se describe el enfoque metodológico adoptado, el cual combina técnicas cualitativas y cuantitativas bajo un esquema de investigación aplicada tecnológica, lo que permitió realizar un análisis integral del problema y de la solución propuesta. Posteriormente, se exponen las fases específicas del proceso de desarrollo, tales como la recolección y preprocesamiento de datos, la selección de algoritmos, el entrenamiento del modelo y la integración del sistema en un entorno controlado, para finalmente evaluarlo en condiciones reales de aula.


\section{Enfoque}

El estudio empleó un enfoque mixto que integró desarrollo tecnológico e investigación experimental, combinando métodos cuantitativos y cualitativos para evaluar tanto el desempeño técnico del sistema como su aplicabilidad en el entorno académico. 

Desde el punto de vista cuantitativo, se midieron indicadores de rendimiento como la precisión del reconocimiento facial, el tiempo promedio de procesamiento por rostro y la tasa de error del sistema. En el componente cualitativo, se consideraron observaciones y percepciones de los usuarios sobre la facilidad de uso, la fiabilidad del registro y la utilidad del sistema en la gestión académica.

El tipo de investigación corresponde a una investigación aplicada con alcance descriptivo–explicativo, orientada al diseño, desarrollo y validación de un sistema funcional de asistencia automatizada mediante reconocimiento facial. 

El diseño adoptado es de tipo experimental, dado que se evaluó el sistema en condiciones reales de aula, controlando variables como iluminación, distancia de captura y número de estudiantes presentes. El carácter longitudinal del estudio permitió analizar el comportamiento y la estabilidad del sistema a lo largo de un periodo académico completo, estableciendo así evidencia empírica sobre su eficacia y consistencia operativa.


\section{Método utilizado para el desarrollo}

El desarrollo de este trabajo se llevó a cabo siguiendo la metodología ágil \textit{Scrum}, adaptada al contexto académico de la investigación. Este enfoque permitió organizar las tareas en ciclos cortos denominados \textit{sprints}, priorizando la entrega progresiva de funcionalidades y la mejora continua a lo largo del proceso.

\textbf{Roles del equipo:} El \textit{Product Owner} fue el profesor Jorge Arrúa, quien definía los requerimientos y evaluaba los entregables en cada revisión. El rol de \textit{Scrum Master} fue desempeñado por Daniel Sanchez, responsable de coordinar las tareas, facilitar la comunicación y asegurar el cumplimiento de los objetivos de cada sprint. El \textit{Development Team} estuvo conformado por Aldo Carrizo y Daniel Sanchez, encargados del desarrollo, pruebas e integración del sistema.

\textbf{Planificación de los sprints:} El desarrollo del sistema se organizó en catorce sprints de aproximadamente dos semanas cada uno, cubriendo el periodo comprendido entre abril y octubre de 2025. Esta planificación permitió avanzar de forma iterativa, incorporando mejoras continuas en cada ciclo de trabajo.
A continuación, se describen las principales actividades desarrolladas en cada fase:

\begin{itemize}
\item \textbf{Sprint 1:} Definición de objetivos, alcance y requerimientos del sistema. Configuración inicial del repositorio en GitHub.
\item \textbf{Sprint 2:} Instalación del entorno de desarrollo y configuración de dependencias para los módulos de detección y reconocimiento facial.
\item \textbf{Sprint 3:} Diseño del Diagrama Entidad-Relación (DER) y creación de la base de datos en PostgreSQL.
\item \textbf{Sprint 4:} Implementación del algoritmo de detección facial utilizando el modelo preentrenado \textit{YOLOv8n-face.pt}, realizando las primeras pruebas con imágenes estáticas para validar su funcionamiento.
\item \textbf{Sprint 5:} Implementación del algoritmo de reconocimiento facial con \textit{DeepFace} (modelo ArcFace) y generación inicial de embeddings faciales.
\item \textbf{Sprint 6:} Pruebas unitarias y evaluación de precisión del modelo de DeepFace. Ajuste de parámetros para reducir errores de identificación.
\item \textbf{Sprint 7:} Integración entre YOLO y DeepFace para el flujo completo de detección y reconocimiento facial.
\item \textbf{Sprint 8:} Desarrollo de las \textit{APIs REST} del backend para usuarios, roles, cursos, asistencias y autenticación.
\item \textbf{Sprint 9:} Implementación de la lógica de registro automático y manual de asistencias. Optimización del rendimiento del sistema.
\item \textbf{Sprint 10:} Desarrollo del \textit{frontend} y diseño del dashboard principal. Implementación del módulo de inicio de sesión.
\item \textbf{Sprint 11:} Creación de los módulos de gestión de alumnos, cursos, horarios y asignaciones dentro del panel administrativo.
\item \textbf{Sprint 12:} Pruebas en entornos aislados y simulaciones en laboratorio para validar la estabilidad del sistema.
\item \textbf{Sprint 13:} Pruebas en el aula, corrección de incidencias, optimización final del código, despliegue controlado y documentación técnica y de usuario.
\end{itemize}


\textbf{Herramientas de gestión:} Para la planificación, seguimiento y control del trabajo se empleó la herramienta \textit{Notion}, que permitió organizar las tareas de acuerdo con su estado de avance (pendiente, en progreso, en revisión y completadas).
Como se observa en la Figura~\ref{fig:scrum}, esta plataforma facilitó la coordinación entre los integrantes del equipo, la priorización de actividades y el registro de los avances en cada \textit{sprint}, brindando una visión clara del progreso general del sistema desarrollado.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.99\textwidth]{imagenes_doc/notion.jpg}
  \caption{Tablero de gestión de tareas del trabajo en Notion, bajo la metodología ágil Scrum Fuente: [Elaboración Propia].}
  \label{fig:scrum}
\end{figure}


\textbf{Reuniones y seguimiento:} El equipo realizó reuniones diarias breves entre los desarrolladores para resolver dificultades técnicas y coordinar actividades. Además, cada dos semanas se llevaron a cabo reuniones presenciales con el profesor Jorge Arrúa para presentar avances, recibir retroalimentación y definir los objetivos del siguiente sprint.

\textbf{Duración del trabajo:} El desarrollo completo del sistema se extendió desde abril hasta octubre de 2025, periodo durante el cual se logró diseñar, integrar, probar y documentar la solución final de reconocimiento facial para el registro de asistencia.


\section{Fases del proceso investigativo}

\begin{itemize}
  \item Identificación de requisitos del trabajo \\
  En esta primera fase se realizó el levantamiento de información mediante una encuesta aplicada a aproximadamente 15 personas, entre docentes y estudiantes de la Facultad Politécnica de la Universidad Nacional del Este (FPUNE). El objetivo fue identificar las principales necesidades y expectativas respecto al control de asistencia automatizado, así como los problemas presentes en el sistema manual existente.  
  A partir de las respuestas obtenidas se definieron los requisitos funcionales y no funcionales del sistema, estableciendo las funcionalidades esenciales, los recursos técnicos requeridos y las condiciones de uso dentro del entorno académico.

  \item Recolección y preparación de datos \\
  Para conformar la base de datos facial, se llevó a cabo una sesión fotográfica dentro del aula donde posteriormente se realizaron las pruebas del sistema.  
  Las imágenes fueron capturadas utilizando un teléfono celular, manteniendo una distancia aproximada de un metro entre la cámara y el rostro de cada participante.  
  Cada usuario fue fotografiado en múltiples posiciones faciales (frontal, ligeramente lateral, mirada hacia abajo o arriba) y con variaciones de accesorios, como anteojos o gorras, con el fin de aumentar la robustez del modelo ante diferentes condiciones.  
  Para garantizar una correcta identificación, se requirió un mínimo de cinco fotografías por participante, aunque la cantidad final de imágenes varió según cada usuario y las condiciones en que fueron tomadas.  
  En total, se obtuvieron imágenes correspondientes a trece personas, las cuales fueron almacenadas cumpliendo las normas éticas del trabajo y con consentimiento verbal informado de los participantes.
  Posteriormente, todas las fotografías fueron sometidas a un proceso de preprocesamiento que incluyó normalización, reducción de ruido y ajustes de brillo y contraste, con el objetivo de mejorar la calidad general del conjunto de datos.

  \item Entrenamiento del modelo \\
  Para la detección facial se empleó un modelo YOLOFace preentrenado, el cual ya incluye una red neuronal convolucional entrenada en grandes conjuntos de rostros.  
  En este trabajo, el modelo fue utilizado como base para realizar la detección de rostros de los usuarios locales, a quienes posteriormente se les generaron representaciones vectoriales (embeddings) mediante la biblioteca DeepFace y el modelo ArcFace.  
  Estos embeddings se almacenan en la base de datos del sistema, permitiendo reconocer posteriormente a cada persona de manera automática durante la toma de asistencia.

  \item Diseño del sistema \\
  Se definió una arquitectura modular que integra los componentes de captura, detección, reconocimiento y registro en la base de datos.  
  El backend fue desarrollado con el framework Flask, encargado de gestionar las solicitudes, procesar las imágenes y registrar los datos en la base de datos PostgreSQL.  
  El frontend fue implementado en React, ofreciendo una interfaz moderna e interactiva que permite visualizar la cámara en tiempo real, gestionar usuarios y consultar asistencias registradas.

\end{itemize}



\section{Análisis y selección de herramientas}

Durante esta fase se realizó un análisis comparativo de diversas tecnologías y librerías aplicadas al desarrollo de sistemas de reconocimiento facial. La selección de herramientas se basó en criterios como precisión, rendimiento, compatibilidad, facilidad de integración y disponibilidad de documentación.

Es importante destacar que las tecnologías elegidas no fueron seleccionadas de manera aleatoria, sino a partir de la experiencia y el conocimiento previo del equipo en el uso de entornos y librerías de visión por computadora. Sin embargo, antes de adoptar una decisión definitiva, se llevaron a cabo pruebas experimentales con distintas alternativas para evaluar su desempeño en las condiciones específicas del trabajo.

Estas evaluaciones permitieron determinar qué herramientas ofrecían los mejores resultados en términos de detección, reconocimiento y velocidad de procesamiento, considerando las limitaciones del hardware disponible y el entorno académico en el que se desarrollaría el sistema.

A continuación, se detallan las principales herramientas seleccionadas:

\begin{itemize}

\item \textbf{Modelos de detección y reconocimiento facial:} \\
Para la etapa de detección de rostros se analizaron diversas alternativas basadas en aprendizaje profundo, considerando factores como la precisión, la velocidad de procesamiento y la capacidad de operar en tiempo real. Tras la comparación de diferentes tecnologías, se determinó que \textit{YOLO-Face} ofrece el mejor equilibrio entre exactitud y eficiencia, motivo por el cual fue seleccionado como modelo principal de detección facial. 

\begin{table}[H]
\centering
\caption{Resultados principales de los modelos de detección\cite{fatima2024yolov8}.}
\label{tab:comparacion-modelos}
\renewcommand{\arraystretch}{1.3}
\normalsize
\setlength{\tabcolsep}{3pt}
\begin{tabular}{|l|p{2cm}|p{2.7cm}|c|p{4.1cm}|}
\hline
\textbf{Modelo} & \textbf{Precisión} & \textbf{Tiempo de Proceso} & \textbf{mAP} & \textbf{Observaciones} \\
\hline
\textbf{YOLOv8}      & 0.71 & \textbf{1 ms}  & \textbf{0.71}  &
Mejor equilibrio entre velocidad y exactitud; ideal para tiempo real. \\
\hline
\textbf{RetinaNet}   & 0.67 & 1.236 s & 0.67 &
Focal Loss mejora el manejo de clases desequilibradas, con menor velocidad. \\
\hline
\textbf{EfficientDet} & 0.55 & 1.0 s & 0.55 &
Eficiente en recursos; recomendado para hardware limitado. \\
\hline
\end{tabular}
\end{table}

\textit{Nota:} mAP (mean Average Precision) indica la precisión promedio media del modelo; valores más altos reflejan mayor exactitud en la detección.


\item \textbf{Comparativa de modelos YOLO para detección facial}

Se evaluaron diferentes versiones de YOLO adaptadas al reconocimiento facial. Finalmente, se seleccionó YOLOv8n-Face por su buen equilibrio entre precisión y velocidad.


\begin{table}[H]
    \centering
    \caption{Comparativa de modelos YOLO para detección facial\cite{yolov8face2024}.}
    \label{tab:comparativa_yolo}
    \renewcommand{\arraystretch}{1.3}
    \small
    \begin{tabular}{|p{3.2cm}|p{1.5cm}|p{1.8cm}|p{5.3cm}|}
        \hline
        \textbf{Modelo} & \textbf{mAP} & \textbf{Precisión} & \textbf{Observaciones} \\ \hline

        YOLOv5n-Face & $\sim$0.31 & $\sim$0.60 & Muy liviano y rápido, pero con menor precisión general.\\ \hline

        YOLOv5s-Face & $\sim$0.34 & $\sim$0.65 & Mejor precisión que v5n. Rendimiento estable en equipos de gama media. \\ \hline

        YOLOv8n-Face & \textbf{0.375} & \textbf{0.625} & Excelente equilibrio entre precisión y velocidad. \\ \hline

        YOLOv8s-Face & $\sim$0.43 & $\sim$0.70 & Mayor precisión con ligero aumento en consumo de recursos. Adecuado para entornos con GPU moderada. \\ \hline

        YOLOv8m-Face & $\sim$0.46 & $\sim$0.72 & Alta precisión y detección robusta, aunque con mayor carga computacional. Ideal para pruebas exhaustivas. \\ \hline
    \end{tabular}
\end{table}


\item \textbf{Evaluación según nivel de dificultad (WIDER Face)}
\sloppy
La tabla presenta el desempeño de distintos modelos de detección facial evaluados en el conjunto de datos WIDER Face, considerando tres niveles de dificultad: fácil, medio y difícil.  
Las condiciones más complejas incluyen rostros pequeños, parcialmente visibles o con iluminación deficiente.  
Los resultados evidencian que la versión YOLOv8-Face mantiene un rendimiento más consistente y robusto frente a escenarios desafiantes.

\begin{table}[H]
    \centering
    \caption{Rendimiento de modelos YOLO en distintos niveles de dificultad (WIDER Face)\cite{yolov8face2024}.}
    \label{tab:rendimiento_dificultad}
    \renewcommand{\arraystretch}{1.25}
    \resizebox{\linewidth}{!}{%
    \begin{tabular}{|p{3.8cm}|c|c|c|}
        \hline
        \textbf{Modelo} & \textbf{Easy (\%)} & \textbf{Medium (\%)} & \textbf{Hard (\%)} \\ \hline
        YOLOv5s–Face & 91.20 & 89.10 & 71.30 \\ \hline
        YOLOv8n–Face & 93.79 & 91.82 & 79.38 \\ \hline
        YOLOv8s–Face & 95.13 & 93.62 & 82.90 \\ \hline
        YOLOv8x–Face & 96.33 & 95.16 & 85.80 \\ \hline
    \end{tabular}}%
\end{table}


\item \textbf{Comparativa de modelos DeepFace}

La tabla muestra distintos modelos compatibles con DeepFace evaluados en cuanto a precisión, velocidad, tamaño, métrica de comparación y sus ventajas y desventajas. Se destaca ArcFace como el más preciso, aunque requiere buen preprocesamiento.


\begin{table}[H]
    \centering
    \caption{Comparativa de modelos de DeepFace\cite{yolov8face2024}.}
    \label{tab:deepface1}
    \renewcommand{\arraystretch}{1.25}
    \resizebox{\linewidth}{!}{%
    \begin{tabular}{|p{3.2cm}|c|c|c|p{3.2cm}|}
        \hline
        \textbf{Modelo} & \textbf{Precisión (LFW)} & \textbf{Velocidad} & \textbf{Tamaño} & \textbf{Métrica} \\ \hline
        ArcFace     & \textbf{99.83\%} & Media      & $\sim$68 MB  & Cosine / Euclídea \\ \hline
        VGG-Face    & 98.78\%          & Lenta      & $\sim$500 MB & Cosine \\ \hline
        Facenet     & 99.20\%          & Rápida     & $\sim$90 MB  & Euclídea \\ \hline
        Facenet512  & 99.25\%          & Media      & $\sim$125 MB & Euclídea \\ \hline
        Dlib        & 96.00\%          & Muy rápida & $\sim$5 MB   & Euclídea \\ \hline
        SFace       & 99.65\%          & Media      & $\sim$100 MB & Cosine \\ \hline
    \end{tabular}}%
\end{table}


\vspace{0.5cm}

\begin{table}[H]
    \centering
    \caption{Comparativa de modelos de DeepFace \cite{yolov8face2024}.}
    \label{tab:deepface2}
    \renewcommand{\arraystretch}{1.25}
    \resizebox{\linewidth}{!}{%
    \begin{tabular}{|p{3cm}|p{5.2cm}|p{5.2cm}|}
        \hline
        \textbf{Modelo} & \textbf{Ventajas principales} & \textbf{Desventajas} \\ \hline
        ArcFace & Alta precisión y robustez ante cambios de pose o luz & Requiere preprocesamiento cuidadoso \\ \hline
        VGG-Face & Modelo base de DeepFace, bien entrenado & Lento y de gran tamaño \\ \hline
        Facenet & Ligero y estable & Menor precisión que ArcFace \\ \hline
        Facenet512 & Versión más precisa de Facenet & Mayor peso del modelo \\ \hline
        Dlib & Muy liviano, ideal para CPU & Precisión limitada \\ \hline
        SFace & Buen desempeño en diversas condiciones & En evaluación por la comunidad \\ \hline
    \end{tabular}}%
\end{table}



\item \textbf{Framework de desarrollo backend:} \\
Para la construcción de la API y la gestión de la lógica del sistema se empleó \textit{Flask}, un microframework de Python que permite desarrollar aplicaciones ligeras y escalables. Su estructura modular facilita la integración con librerías de visión por computadora y bases de datos.

\item \textbf{Framework de desarrollo frontend:} \\
La interfaz de usuario fue desarrollada en \textit{React}, permitiendo una interacción dinámica y fluida con el sistema. Su arquitectura basada en componentes favorece la reutilización del código y la visualización en tiempo real de los registros de asistencia.

\item \textbf{Base de datos:} \\
Se utilizó \textit{PostgreSQL} para el almacenamiento estructurado de la información, incluyendo los registros de asistencia y datos de usuarios. Su robustez y soporte para operaciones concurrentes la convierten en una opción óptima para sistemas de este tipo.

\item \textbf{Herramienta de pruebas de API:} \\
Durante el proceso de desarrollo e integración, se empleó \textit{Postman} para realizar pruebas de comunicación entre el frontend y el backend. Esta herramienta permitió verificar el correcto funcionamiento de las rutas, peticiones y respuestas del sistema antes de su despliegue final.

\end{itemize}

La combinación de estas herramientas permitió un desarrollo eficiente, garantizando la integración adecuada entre los módulos de detección, reconocimiento y gestión de asistencia dentro del sistema.


\section{Recolección de datos}

Para el funcionamiento del sistema de reconocimiento facial, fue necesario disponer de un conjunto de imágenes faciales representativas de los usuarios. Durante el registro de cada nuevo usuario, el sistema captura un mínimo de cinco imágenes desde diferentes ángulos y expresiones, con el fin de mejorar la capacidad de reconocimiento en distintas condiciones de uso.

Las imágenes se obtuvieron con cámaras externas (teléfonos móviles, webcams o cámaras digitales) y posteriormente se cargan al sistema mediante el módulo de registro de nuevos estudiantes. Durante este proceso se verifica el formato y un tamaño mínimo recomendado para preservar los rasgos faciales. Cada archivo se organizó en una carpeta individual asociada al usuario correspondiente y se nombra de forma sistemática, facilitando su identificación y el procesamiento posterior.

Todas las capturas fueron realizadas cumpliendo las normas éticas establecidas para el desarrollo del trabajo y con consentimiento verbal informado de los participantes, garantizando el uso responsable y confidencial de los datos obtenidos.

En la Figura~\ref{fig:rostro_original} se muestra un ejemplo de una imagen facial capturada al natural durante el proceso de registro, mientras que la Figura~\ref{fig:rostro_normalizado} presenta el resultado tras aplicar el preprocesamiento correspondiente.

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{imagenes_doc/sin_normalizar.png}
    \caption{Ejemplo de imagen facial capturada al natural Fuente: [Elaboración Propia].}
    \label{fig:rostro_original}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{imagenes_doc/dataset_rostros.jpg}
    \caption{Ejemplo de imagen facial luego del proceso de normalización Fuente: [Elaboración Propia].}
    \label{fig:rostro_normalizado}
\end{figure}


\section{Entrenamiento del modelo}

El entrenamiento del modelo constituye una etapa esencial del sistema, ya que permite obtener representaciones numéricas únicas de cada rostro a partir de las imágenes registradas. En esta fase se utilizaron técnicas de aprendizaje profundo orientadas a la verificación facial, capaces de generar \textit{embeddings} discriminativos y estables frente a variaciones de iluminación, expresión o ángulo de captura, garantizando así una identificación confiable y precisa.

\subsection*{Configuración del entorno de entrenamiento}

\begin{itemize}
    \item \textbf{Lenguaje:} Python 3.12.11
    \item \textbf{Librerías principales:} DeepFace, OpenCV, NumPy, Ultralytics
    \item \textbf{Entorno:} Visual Studio Code (ejecución local con GPU o CPU)
    \item \textbf{Equipo utilizado:} AMD Ryzen 7 7735HS, 16 GB RAM, SSD 512 GB, Windows 11 Home
    \item \textbf{Dispositivo de captura:} Cámara web HD 720p para obtención de imágenes faciales en pruebas locales.
    \item \textbf{Condiciones de iluminación:} Pruebas realizadas entre 300–500\,lux para simular entornos reales de aula. 
La iluminación fue cuantificada utilizando un luxómetro digital portátil modelo \textit{Dr.meter LX1330B}, con lecturas tomadas a una altura aproximada de 1 a 1.5 metros, equivalente al nivel de una mesa, antes de cada sesión experimental.
    \item \textbf{Estructura del dataset:} Imágenes organizadas por usuario en carpetas dentro de \texttt{raw\_faces/} (imágenes originales) y \texttt{known\_faces/} (rostros recortados).
    \item \textbf{Modelo de embeddings:} \textit{ArcFace} (512 dimensiones).
    \item \textbf{Métrica de similitud:} Distancia coseno para comparar embeddings entre el rostro capturado y los centroides registrados.
    \item \textbf{Umbral de decisión:} 0.45 como límite para determinar coincidencia; valores superiores clasifican el rostro como desconocido.
    \item \textbf{Estrategia de detección:} \texttt{enforce\_detection = False} y \texttt{detector\_backend = "skip"} para acelerar el procesamiento al asumir que los rostros ya se encuentran recortados.
    \item \textbf{Cálculo de similitud:} Conversión de la distancia coseno a porcentaje mediante una escala lineal para interpretación del resultado.
    \item \textbf{Formato de salida:} Archivos JSON con los embeddings y centroides por usuario.
\end{itemize}

\subsection*{Proceso de entrenamiento}

El proceso de entrenamiento fue automatizado mediante el script \texttt{train\_faces.py}, compuesto por dos fases principales: el preprocesamiento de imágenes y la generación de representaciones faciales.

\begin{enumerate}
    \item \textbf{Registro del usuario e ingreso de imágenes:} \\
    Desde la interfaz web, un usuario administrativo registra a un nuevo alumno y carga sus fotografías faciales. Cada conjunto de imágenes se guarda en una carpeta individual dentro del directorio \texttt{raw\_faces/}, identificada por el número de cédula del alumno (CI).

    \item \textbf{Detección y recorte facial:} \\
    Para garantizar que solo se utilicen regiones válidas del rostro, el sistema emplea el modelo \textit{YOLO-Face}. Este detecta automáticamente el rostro en cada imagen, recorta la región correspondiente y la redimensiona a \textit{160×160 píxeles}, almacenando el resultado en \texttt{known\_faces/}. Este paso estandariza las entradas, asegurando consistencia en las condiciones de entrenamiento.

    \item \textbf{Generación de embeddings con DeepFace:} \\
    Cada imagen procesada se analiza mediante \textit{DeepFace} utilizando el modelo \textit{ArcFace}, el cual genera un vector numérico de características que describe de forma única los rasgos faciales del individuo. \\
    Para cada alumno, el sistema genera un archivo JSON individual con su número de cédula como nombre (por ejemplo, \texttt{5144692.json}), que contiene todos los embeddings derivados de sus imágenes.

    \item \textbf{Cálculo y actualización de centroides:} \\
    Posteriormente, se calcula el \textit{centroide} del alumno, representando el promedio robusto de todos sus embeddings. Este vector resume los rasgos característicos del rostro y facilita comparaciones futuras. \\
    A diferencia de un reentrenamiento completo, el sistema implementa una actualización incremental: cuando se agrega un nuevo alumno o se añaden más imágenes a un registro existente, el sistema no recalcula todos los centroides, sino que actualiza únicamente el correspondiente al usuario modificado. De este modo, se optimiza el rendimiento y se conserva el historial de entrenamiento.

    \item \textbf{Almacenamiento estructurado:} \\
    \sloppy
    Los embeddings individuales se guardan en el directorio \texttt{embeddings/}, mientras que el archivo \texttt{centroids.json} mantiene un índice general con los centroides y estadísticas de todos los usuarios. Esta estructura facilita la expansión progresiva del sistema sin afectar los registros previos.
\end{enumerate}


\section{Diseño de la aplicación}
\subsection{Arquitectura del sistema}

El sistema fue diseñado bajo el enfoque cliente-servidor, utilizando una arquitectura basada en \textit{REST API} para permitir la comunicación fluida entre el \textit{Frontend} (cliente) y el \textit{Backend} (servidor).  
Esta arquitectura facilita la escalabilidad, el mantenimiento y la integración con futuras funcionalidades del sistema de reconocimiento facial.

El sistema se compone de tres módulos principales:

\begin{itemize}
    \item \textbf{Frontend:} desarrollado en \textit{React.js}, se encarga de la interfaz gráfica y la interacción directa con el usuario. Permite registrar nuevos estudiantes, gestionar materias, asignarlas a los alumnos, capturar o subir fotografías faciales, consultar los registros de asistencia y cargar justificativos en caso de inasistencia. Su diseño adaptable y responsivo garantiza una experiencia de uso fluida tanto en equipos de escritorio como en dispositivos móviles.

    \item \textbf{Backend:} implementado en \textit{Flask (Python)}, maneja la lógica del sistema, las rutas de la API y la comunicación con la base de datos. Integra los módulos de detección facial con \textit{YOLO-Face} y el reconocimiento mediante \textit{DeepFace (modelo ArcFace)}, además de gestionar el almacenamiento de embeddings, centroides y registros de asistencia.

    \item \textbf{Base de datos:} construida en \textit{PostgreSQL}, almacena de forma estructurada la información de usuarios, cursos, asistencias y datos biométricos. Se eligió por su robustez, soporte de transacciones concurrentes y compatibilidad con entornos productivos en la nube.
\end{itemize}
\subsection{Integración de IA y Reconocimiento Facial}

El sistema combina técnicas avanzadas de detección y reconocimiento facial mediante redes neuronales convolucionales. Para la primera etapa, se emplea un modelo especializado en la identificación y localización de rostros en tiempo real. Una vez detectada la región de interés, esta se recorta automáticamente y se normaliza a un tamaño estándar de 160×160 píxeles, garantizando uniformidad en las condiciones de entrada.

En la siguiente fase, el rostro procesado es transformado en una representación numérica o vector de características, la cual permite distinguir de manera precisa a cada individuo. Estos vectores se almacenan y se utilizan posteriormente para la verificación e identificación de los usuarios registrados.

\textbf{El flujo general de procesamiento es el siguiente:}

\begin{enumerate}
    \item Captura o carga de imagen facial desde la interfaz.
    \item Detección automática de rostros mediante YOLO-Face.
    \item Recorte y normalización del rostro detectado.
    \item Generación de embeddings con DeepFace (modelo ArcFace).
    \item Registro automático de la asistencia según el resultado del proceso.
\end{enumerate}

El proceso completo se ejecuta desde el backend, implementado en \textit{Flask (Python)}, que coordina la detección, el reconocimiento y el registro de resultados en la base de datos \textit{PostgreSQL}.  
Esta integración permite un flujo automatizado, eficiente y en tiempo real, garantizando precisión en la identificación de los estudiantes durante la toma de asistencia.

\subsection{Relevamiento de requisitos}

El relevamiento de requisitos se llevó a cabo mediante la aplicación de encuestas dirigidas a profesores y estudiantes de la Facultad Politécnica de la Universidad Nacional del Este (FPUNE), con el objetivo de identificar las principales necesidades y expectativas respecto al control de asistencia en el entorno académico.  

Los resultados obtenidos permitieron comprender las mejoras asociadas a los métodos tradicionales de control de asistencia tales como pérdida de tiempo, errores humanos y registros inconsistentes, y sirvieron de base para definir los requisitos funcionales y no funcionales del sistema.

\subsubsection*{Requisitos funcionales (RF)}

Los requisitos funcionales representan las acciones que el sistema debe realizar para cumplir su propósito. A partir del análisis de las encuestas y la definición de objetivos, se identificaron los siguientes requisitos funcionales, resumidos en la Tabla~\ref{tab:rf_sistema}.


\begin{table}[H]
\centering
\caption{Requisitos funcionales del sistema Fuente: [Elaboración Propia].}
\label{tab:rf_sistema}
\begin{tabular}{|c|p{11cm}|}
\hline
\textbf{Código} & \textbf{Requisito funcional} \\ \hline
RF1 & El sistema debe permitir el registro de nuevos estudiantes y docentes mediante una interfaz web. \\ \hline
RF2 & El sistema debe permitir la carga de imágenes faciales de los estudiantes para el entrenamiento del modelo. \\ \hline
RF3 & El sistema debe detectar automáticamente los rostros presentes en video. \\ \hline
RF4 & El sistema debe generar embeddings faciales y almacenarlos junto con la información del usuario. \\ \hline
RF5 & El sistema debe registrar automáticamente la asistencia al reconocer un rostro en tiempo real. \\ \hline
RF6 & El sistema debe permitir consultar y justificar asistencias desde la interfaz web. \\ \hline
\end{tabular}
\end{table}



\subsubsection*{Requisitos no funcionales (RNF)}
\sloppy
Los requisitos no funcionales establecen las condiciones de calidad, rendimiento y usabilidad que el sistema debe cumplir para garantizar un funcionamiento eficiente y confiable. La Tabla~\ref{tab:rnf_sistema} presenta los principales requisitos no funcionales definidos.


\begin{table}[H]
\centering
\caption{Requisitos no funcionales del sistema Fuente: [Elaboración Propia].}
\label{tab:rnf_sistema}
\begin{tabular}{|c|p{11cm}|}
\hline
\textbf{Código} & \textbf{Requisito no funcional} \\ \hline
RNF1 & El sistema debe procesar la detección y reconocimiento facial en menos de 3 segundos por usuario. \\ \hline
RNF2 & El sistema debe mantener una precisión mínima del 80\% en la identificación de rostros. \\ \hline
RNF3 & La interfaz debe ser intuitiva, responsiva y accesible desde dispositivos móviles y computadoras. \\ \hline
RNF4 & La base de datos debe garantizar integridad, seguridad y respaldo de los registros. \\ \hline
\end{tabular}
\end{table}



\subsection{Caso de Uso Principal del Sistema}

En la Figura~\ref{fig:caso_uso_principal} se representa el caso de uso principal del sistema, en el cual interactúan dos actores principales: el docente y el alumno.  

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{capitulo_04/imagenes/5.png}
    \caption{Caso de uso principal del sistema Fuente: [Elaboración Propia].}
    \label{fig:caso_uso_principal}
\end{figure}


\subsubsection{Descripción general del caso de uso}

El sistema distingue dos tipos de usuarios con diferentes niveles de acceso: el Docente y el Estudiante.

\textbf{Docente:} tiene acceso completo a todas las funcionalidades del sistema. Puede registrar estudiantes, cursos, horarios y roles; asignar materias; registrar asistencias mediante el reconocimiento facial; y acceder al panel principal (\textit{Dashboard}) con los reportes y estadísticas. Además, puede administrar los datos del sistema y supervisar su funcionamiento general, actuando como un administrador.

\textbf{Estudiante:} dispone de un acceso limitado al sistema, pudiendo ingresar únicamente al \textit{Dashboard} para visualizar sus registros de asistencia y verificar su historial personal. No posee permisos de edición ni administración de datos.

\subsection{Definición de la Base de Datos y Tablas}

Para la gestión eficiente de la información del sistema, se implementó una base de datos relacional compuesta por varias tablas que permiten almacenar, vincular y administrar los datos de usuarios, participantes, cursos, horarios y registros de asistencia.  
La estructura fue diseñada para garantizar integridad referencial, seguridad y escalabilidad, facilitando futuras ampliaciones y adaptaciones del sistema.

\subsubsection*{Modelo de entidad-relación}

La base de datos se compone de siete entidades principales: \textit{roles}, \textit{users}, \textit{participants}, \textit{courses}, \textit{course\_schedules}, \textit{participant\_courses} y \textit{attendances}.  
Estas entidades reflejan la estructura funcional del sistema, permitiendo asociar estudiantes a cursos, registrar asistencias y controlar los horarios académicos.  
En la Figura~\ref{fig:der_sistema} se muestra el diagrama entidad-relación correspondiente.


\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{capitulo_04/imagenes/1.png}
    \caption{Diagrama de Entidad–Relación del sistema Fuente: [Elaboración Propia].}
    \label{fig:der_sistema}
\end{figure}


\subsubsection*{Diccionario de datos}

Con el fin de documentar detalladamente la estructura de cada tabla, se elaboró un diccionario de datos que especifica los atributos, tipos de datos y relaciones existentes. A continuación, se describen las tablas principales que conforman la base de datos.

La Tabla 4.8 presenta el diccionario de datos correspondiente a la tabla roles de la base de datos. En ella se describen los atributos que conforman esta entidad, el tipo de dato utilizado para su almacenamiento y la función que cumple cada campo dentro del sistema.
\begin{table}[H]
\centering
\caption{Diccionario de datos de la tabla \textit{roles} Fuente: [Elaboración Propia].}
\begin{tabular}{|l|l|p{7.4cm}|}
\hline
\textbf{Atributo} & \textbf{Tipo de Dato} & \textbf{Descripción} \\ \hline
id & SERIAL (PK) & Identificador único del rol. \\ \hline
description & VARCHAR(120) & Nombre o descripción del rol asignado al usuario. \\ \hline
\end{tabular}
\end{table}

La Tabla 4.9 detalla la estructura de la tabla users utilizada en la base de datos. En ella se especifican los atributos que identifican a cada usuario del sistema, así como sus tipos de datos y la función que cumple cada campo dentro del modelo relacional.
\begin{table}[H]
\centering
\caption{Diccionario de datos de la tabla \textit{users} Fuente: [Elaboración Propia].}
\begin{tabular}{|l|l|p{7.5cm}|}
\hline
\textbf{Atributo} & \textbf{Tipo de Dato} & \textbf{Descripción} \\ \hline
id & SERIAL (PK) & Identificador único del usuario. \\ \hline
username & VARCHAR(120) & Nombre de usuario utilizado para iniciar sesión. \\ \hline
password & TEXT & Contraseña encriptada del usuario. \\ \hline
role\_id & INT (FK) & Referencia al rol del usuario en la tabla \textit{roles}. \\ \hline
\end{tabular}
\end{table}

La Tabla 4.10 presenta el diccionario de datos de la tabla participants, donde se detallan los atributos utilizados para registrar la información principal de cada estudiante o participante dentro del sistema. Se incluyen los tipos de datos empleados y la función específica que cumple cada campo, así como su relación con otras tablas del modelo.
\begin{table}[H]
\centering
\caption{Diccionario de datos de la tabla \textit{participants} Fuente: [Elaboración Propia].}
\begin{tabular}{|l|l|p{7.4cm}|}
\hline
\textbf{Atributo} & \textbf{Tipo de Dato} & \textbf{Descripción} \\ \hline
id & SERIAL (PK) & Identificador único del participante. \\ \hline
full\_name & VARCHAR(120) & Nombre completo del estudiante o participante. \\ \hline
ci & INT & Número de cédula de identidad del participante. \\ \hline
occupation & VARCHAR(50) & Rol académico del participante (por ejemplo, alumno). \\ \hline
user\_id & INT (FK) & Relación con el usuario correspondiente en la tabla \textit{users}. \\ \hline
\end{tabular}
\end{table}


La Tabla 4.11 presenta el diccionario de datos de la tabla courses, especificando los campos utilizados para identificar cada curso, su denominación, la carrera a la que pertenece y el semestre correspondiente.
\begin{table}[H]
\centering
\caption{Diccionario de datos de la tabla \textit{courses} Fuente: [Elaboración Propia].}
\begin{tabular}{|l|l|p{7.5cm}|}
\hline
\textbf{Atributo} & \textbf{Tipo de Dato} & \textbf{Descripción} \\ \hline
id & SERIAL (PK) & Identificador único del curso. \\ \hline
name & VARCHAR(120) & Nombre de la materia o curso. \\ \hline
career & VARCHAR(120) & Carrera o programa académico al que pertenece el curso. \\ \hline
semester & INT & Semestre académico correspondiente. \\ \hline
\end{tabular}
\end{table}




La Tabla 4.12 presenta el diccionario de datos de la tabla course\_schedules, donde se detallan los campos utilizados para registrar los horarios asignados a cada curso. Se especifican los atributos que conforman esta entidad, los tipos de datos empleados y la función de cada campo dentro del sistema académico.
\begin{table}[H]
\centering
\caption{Diccionario de datos de la tabla \textit{course\_schedules} Fuente: [Elaboración Propia].}
\begin{tabular}{|l|l|p{7.7cm}|}
\hline
\textbf{Atributo} & \textbf{Tipo de Dato} & \textbf{Descripción} \\ \hline
id & SERIAL (PK) & Identificador único del horario. \\ \hline
course\_id & INT (FK) & Referencia al curso correspondiente. \\ \hline
day & VARCHAR(20) & Día de la semana en que se dicta el curso. \\ \hline
schedule & VARCHAR(50) & Horario asignado al curso. \\ \hline
classroom & VARCHAR(50) & Aula o sala asignada. \\ \hline
\end{tabular}
\end{table}


La Tabla 4.13 muestra el diccionario de datos de la tabla participant\_courses, encargada de representar la relación entre los participantes y los cursos en los que están matriculados. En ella se especifican los atributos que conforman esta entidad, los tipos de datos utilizados y las referencias a las tablas vinculadas.
\begin{table}[H]
\centering
\caption{Diccionario de datos de la tabla \textit{participant\_courses} Fuente: [Elaboración Propia].}
\begin{tabular}{|l|l|p{7.1cm}|}
\hline
\textbf{Atributo} & \textbf{Tipo de Dato} & \textbf{Descripción} \\ \hline
id & SERIAL (PK) & Identificador único de la relación. \\ \hline
participant\_id & INT (FK) & Referencia al participante matriculado. \\ \hline
course\_id & INT (FK) & Referencia al curso correspondiente. \\ \hline
\end{tabular}
\end{table}



La Tabla 4.14 presenta el diccionario de datos de la tabla attendances, utilizada para almacenar los registros de asistencia generados por el sistema. En ella se describen los atributos que conforman esta entidad, los tipos de datos empleados y las relaciones que mantiene con otras tablas del modelo académico.
\begin{table}[H]
\centering
\caption{Diccionario de datos de la tabla \textit{attendances} Fuente: [Elaboración Propia].}
\begin{tabular}{|l|l|p{7.1cm}|}
\hline
\textbf{Atributo} & \textbf{Tipo de Dato} & \textbf{Descripción} \\ \hline
id & SERIAL (PK) & Identificador único del registro de asistencia. \\ \hline
participant\_id & INT (FK) & Referencia al participante que asiste al curso. \\ \hline
course\_id & INT (FK) & Referencia al curso al que pertenece la asistencia. \\ \hline
date & DATE & Fecha en la que se registra la asistencia. \\ \hline
observations & TEXT & Observaciones adicionales o justificación de inasistencia. \\ \hline
\end{tabular}
\end{table}


\subsection{Diagrama de flujo del funcionamiento del sistema}

La Figura~\ref{fig:flujo_sistema} muestra el diagrama de flujo que describe el funcionamiento general del sistema de asistencia. En él se ilustran las etapas principales del proceso automatizado, desde la carga de imágenes de los estudiantes hasta el registro final en la base de datos.

El proceso inicia con la carga de las imágenes faciales a través de la interfaz, las cuales son procesadas para generar representaciones numéricas que permiten su posterior identificación. Luego, el sistema activa la cámara y ejecuta la detección y verificación en tiempo real. Cuando un rostro coincide con un registro existente, el estudiante es marcado como presente; de lo contrario, se clasifica como desconocido, garantizando un control más preciso del registro de asistencia.

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{capitulo_04/imagenes/2.png}
    \caption{Diagrama de flujo del funcionamiento del sistema Fuente: [Elaboración Propia].}
    \label{fig:flujo_sistema}
\end{figure}



\subsection{Desarrollo del backend}

El backend del sistema fue desarrollado con un enfoque modular y ligero, orientado a gestionar la comunicación entre la interfaz web, la base de datos y los procesos de reconocimiento facial en tiempo real. Se implementó utilizando el framework Flask, basado en el lenguaje Python, debido a su simplicidad, flexibilidad y compatibilidad con bibliotecas de visión por computadora como OpenCV, TensorFlow y DeepFace.

La estructura del backend se organiza en carpetas según la funcionalidad de cada componente. En la raíz se encuentran los archivos principales del sistema, como \texttt{main.py}, encargado de inicializar la aplicación Flask, configurar las rutas y ejecutar el servidor, y \texttt{api.py}, que centraliza los endpoints de la API.  
El directorio \texttt{routes} contiene los módulos de rutas organizados mediante \textit{Blueprints}, lo que permite mantener una arquitectura limpia, separando las responsabilidades de cada parte del sistema, como la gestión de usuarios, cursos, roles y asistencias.  
El módulo \texttt{database} se encarga de la conexión con la base de datos PostgreSQL, gestionando las operaciones de lectura y escritura, así como las relaciones entre entidades.  

El núcleo del reconocimiento facial se encuentra en la carpeta \texttt{recognition}, que contiene los scripts \texttt{face\_recognizer.py} y \texttt{helpers.py}, junto con subcarpetas destinadas a los conjuntos de datos faciales. Dentro de esta estructura se incluyen las carpetas \texttt{raw\_faces} (imágenes originales capturadas), \texttt{known\_faces} (rostros previamente registrados) y \texttt{embeddings} (vectores de características generados para cada usuario).  
En este módulo se realiza el procesamiento de imágenes, aplicando el modelo de detección YOLOFace para localizar los rostros y el modelo ArcFace, a través de la biblioteca DeepFace, para generar y comparar los vectores de características de cada participante.  

El proceso de entrenamiento y actualización de los vectores faciales se realiza mediante los scripts \texttt{train\_faces.py} y \texttt{train\_faces\_old.py}, los cuales permiten recalcular los vectores cuando se incorporan nuevos usuarios o se actualizan los datos existentes. Este mecanismo garantiza que el sistema mantenga la capacidad de reconocer a los estudiantes incluso cuando se amplía la base de datos.

Las principales funcionalidades implementadas en esta capa incluyen:
\begin{itemize}
    \item Exposición de endpoints RESTful: se definieron rutas HTTP para el manejo de usuarios, roles, cursos, horarios, asistencias y reconocimiento facial, enviando y recibiendo datos en formato JSON. Estas rutas facilitan la comunicación entre el frontend y el backend mediante solicitudes asincrónicas.
    \item Procesamiento y reconocimiento facial: las imágenes capturadas desde el frontend son enviadas al backend, donde se ejecutan las etapas de detección, normalización y reconocimiento facial. Si la similitud entre el rostro detectado y los vectores registrados supera el umbral definido, se registra automáticamente la asistencia correspondiente en la base de datos.
    \item Gestión de usuarios y autenticación: se implementaron rutas dedicadas para el registro e inicio de sesión, controlando el acceso a las funciones administrativas del sistema mediante validaciones en el servidor.
    \item Interacción con la base de datos: toda la información sobre usuarios, estudiantes, cursos, horarios y asistencias se gestiona en la base de datos PostgreSQL mediante operaciones CRUD estructuradas y validadas desde la API.
\end{itemize}


\begin{figure}[H]
  \centering
  \includegraphics[width=1\textwidth, height=13cm, keepaspectratio]{imagenes_doc/backend_estructura.png}
  \caption{Estructura general del trabajo en Visual Studio Code Fuente: [Elaboración Propia].}
  \label{fig:backend_estructura}
\end{figure}




\subsection{Desarrollo del frontend}

El frontend del sistema fue desarrollado con un enfoque modular basado en componentes reutilizables, lo que permitió construir una interfaz dinámica, responsiva y visualmente coherente.  
El diseño prioriza la claridad y la facilidad de uso, garantizando una navegación intuitiva entre las diferentes secciones y funcionalidades.


El desarrollo de la interfaz se organizó en módulos funcionales, cada uno representando una parte específica del sistema. La estructura general se compone de:
\begin{itemize}
  \item \textbf{Componentes de presentación:} responsables de mostrar la información y recibir datos de entrada mediante formularios, tablas y botones.
  \item \textbf{Ruteo de navegación:} implementado con \texttt{react-router-dom}, para permitir la transición fluida entre las distintas vistas del sistema.
  \item \textbf{Servicios:} encargados de establecer la comunicación con la REST API del backend, gestionando peticiones HTTP de forma centralizada.
  \item \textbf{Gestión de estado:} realizada mediante los hooks de React (\texttt{useState}, \texttt{useEffect}), asegurando la actualización dinámica de la información visualizada por el usuario.
\end{itemize}

\paragraph{Criterios de diseño de la interfaz.}
\begin{itemize}
  \item Consistencia visual en todos los módulos mediante el uso de componentes y clases de Bootstrap.
  \item Diseño \textit{responsive}, garantizando un correcto funcionamiento en diferentes resoluciones y dispositivos.
  \item Retroalimentación visual inmediata ante las acciones del usuario (por ejemplo, mediante alertas o indicadores de carga).
  \item Accesibilidad básica mediante etiquetas y descripciones que facilitan la interacción.
\end{itemize}

\paragraph{Flujo de uso.}
\begin{itemize}
  \item \textbf{Inicio de sesión:} los usuarios acceden al sistema ingresando sus credenciales personales a través de una interfaz simple y clara, garantizando que únicamente el personal autorizado pueda utilizar las funcionalidades.
  \item \textbf{Registro de nuevos usuarios:} se dispone de un formulario que permite crear nuevas cuentas, asignar roles y definir permisos.
  \item \textbf{Uso de los módulos principales:} dentro del panel lateral de navegación se encuentran las opciones Dashboard, {Reconocimiento Facial}, {Registrar Estudiante}, {Registrar Rol}, {Registrar Curso}, {Registrar Horario}, {Registrar Asistencia} y {Asignar Curso}.
\end{itemize}



\subsection{Líneas de código}

En el Anexo B se presentan los fragmentos de código más relevantes del sistema, junto con una descripción que explica su función y la lógica que implementan dentro del proceso de reconocimiento facial. Los ejemplos incluyen tanto los módulos encargados del entrenamiento de los rostros registrados como aquellos utilizados en la detección y el reconocimiento en tiempo real.

Asimismo, se detalla la forma en que cada componente contribuye al flujo general del sistema, desde la lectura y preparación de imágenes hasta la generación de embeddings, la comparación con la base de datos y la identificación de los estudiantes en el aula. De esta manera, el anexo brinda una visión clara de la implementación técnica y facilita la comprensión del funcionamiento interno del sistema.
% En el preámbulo asegúrate de tener:
% \usepackage{tabularx}
% \usepackage{array}      % para m{...}
% \usepackage{multirow}

\section{Métricas de Evaluación}
La Tabla 4.15 presenta las métricas consideradas durante el desarrollo y la evaluación del sistema. Estas métricas se organizan por categoría e incluyen tanto indicadores relacionados con el conjunto de datos como parámetros de entrenamiento, desempeño del reconocimiento facial, uso de recursos y validación en condiciones reales.
\begin{table}[H]
\centering
\caption{Métricas consideradas en el desarrollo y evaluación del sistema Fuente: [Elaboración Propia].}
\label{tab:metricas-sistema}

\footnotesize
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.2}

\begin{tabularx}{\linewidth}{|>{\centering\arraybackslash}m{2.3cm}|
                                 >{\raggedright\arraybackslash}m{3cm}|
                                 >{\raggedright\arraybackslash}m{3.7cm}|
                                 >{\raggedright\arraybackslash}m{3.5cm}|}
\hline
\textbf{Categoría} & \textbf{Métrica} & \textbf{Descripción breve} & \textbf{Criterio de éxito} \\ \hline

\multirow{3}{*}{\shortstack{Datos del\\ conjunto}}
& Nº total de imágenes
& Imágenes usadas en entrenamiento y validación.
& $\ge 60$ imágenes recopiladas. \\ \cline{2-4}
& Diversidad de condiciones visuales
& Variación de luz, ángulo y expresión.
& $\ge 3$ variaciones (luz, ángulo y expresión). \\ \cline{2-4}
& Nº de participantes registrados
& Estudiantes con rostro registrado en el sistema.
& $\ge 10$ estudiantes registrados. \\ \hline

\multirow{2}{*}{\shortstack{Entrenamiento}}
& Precisión
& Porcentaje de aciertos durante el entrenamiento.
& $\ge 80\%$ de precisión. \\ \cline{2-4}
& Pérdida 
& Error medio por usuario registrado.
& Tendencia decreciente durante el entrenamiento. \\ \hline

\multirow{3}{*}{\shortstack{Reconocimiento\\ facial}}
& Tasa de reconocimiento correcto
& Porcentaje de aciertos en pruebas.
& $\ge 80\%$ de aciertos. \\ \cline{2-4}
& Tasa de falsos positivos/negativos
& Porcentaje de detecciones erróneas u omitidas.
& $\le 20\%$ (valor máximo tolerable). \\ \cline{2-4}
& Tiempo promedio de proceso (ms)
& Tiempo por rostro para detectar y verificar.
& $\le 250\,\text{ms}$ por rostro. \\ \hline

Rendimiento global
& Uso promedio de CPU/GPU
& Utilización media de recursos durante la ejecución.
& $\le 70\%$ de uso de CPU (sin GPU). \\ \hline

Validación práctica
& Tasa de detección real
& Porcentaje de aciertos en condiciones reales de aula.
& $\ge 75\%$ de aciertos. \\ \hline

\end{tabularx}
\end{table}



\subsection{Usabilidad}

Para evaluar la usabilidad y el nivel de satisfacción de los usuarios, se definieron variables operacionales basadas en los principios de experiencia de usuario, considerando la facilidad de uso, la eficiencia, la confiabilidad y la aceptación general de la herramienta desarrollada.  

Como método de medición se utilizó el cuestionario System Usability Scale (SUS), ampliamente reconocido por su efectividad para obtener un puntaje global de usabilidad. La evaluación se realizó con un grupo de usuarios que interactuó con las principales funcionalidades, incluyendo el registro de cuentas, la toma de asistencia y la consulta de reportes en el entorno web.  

Los resultados permitieron determinar el nivel de aceptación general y detectar oportunidades de mejora relacionadas con la interfaz y la navegación. Los datos consolidados y las respuestas individuales se presentan en el Anexo A.

